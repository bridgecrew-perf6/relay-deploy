[Unit]
Description=Relay webserver

[Service]
User=relay
Group=relay
ExecStart=/opt/relay/deno/bin/deno run --allow-net=127.0.0.1:{{ relay_web_port }} --allow-read=/opt/relay/web/var/settings.js /opt/relay/web/webserver.ts
SyslogIdentifier=relay

[Install]
WantedBy=multi-user.target
